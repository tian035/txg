name: probe-official-images

on:
  issue_comment:
    types: [created]

permissions:
  contents: read

jobs:
  probe:
    if: contains(github.event.comment.body, 'RUN_PROBE_IMAGES')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - run: python probe_images.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: probe-artifact
          path: probe_artifact
